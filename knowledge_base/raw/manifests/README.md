# Source Manifests

Store optional metadata manifests for files in `../sources/`.

One file per source is recommended:

```text
<source_id>.md
```

or

```text
<source_id>.json
```

Use this metadata to support citation quality, filtering, and ingestion planning.
